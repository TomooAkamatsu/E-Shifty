import { MotionProps } from "../../../motion";
import { LayoutState, VisualState } from "../../utils/state";
import { DOMVisualElementOptions, HTMLMutableState } from "../types";
export declare function buildHTMLStyles(state: HTMLMutableState, { values: latest, projection }: VisualState, layoutState: LayoutState, options: DOMVisualElementOptions, transformTemplate?: MotionProps["transformTemplate"]): void;
