import { SVGMutableState, DOMVisualElementOptions } from "../types";
import { MotionProps } from "../../../motion/types";
import { LayoutState, VisualState } from "../../utils/state";
/**
 * Build SVG visual attrbutes, like cx and style.transform
 */
export declare function buildSVGAttrs(state: SVGMutableState, { values, projection }: VisualState, layoutState: LayoutState, options: DOMVisualElementOptions, transformTemplate?: MotionProps["transformTemplate"]): void;
