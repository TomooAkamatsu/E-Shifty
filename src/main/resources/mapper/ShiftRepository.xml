<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- Mapperとxmlのマッピング -->
<mapper namespace="com.example.sma.infrastructure.shift.ShiftRepository">

    <select id="findShift" resultType="Shift">
        SELECT
            *
        FROM
            shift
        WHERE
            shift.employee_id = #{employeeId}
        AND
            YEAR(date) = #{year}
        AND
            MONTH(date) = #{month}
        ORDER BY
            shift.date ASC
    </select>

    <select id="findAllShiftPattern" resultType="ShiftPattern">
        SELECT
            *
        FROM
            shift_pattern
    </select>

    <select id="findAllVacationRequest" resultType="VacationRequest">
        SELECT
            *
        FROM
            vacation_request
        WHERE
            employee_id = #{employeeId}
        AND
            YEAR(request_date) = #{year}
        AND
            MONTH(request_date) = #{month}
    </select>

</mapper>
